<Page
    x:Class="Client.initializeChair"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--<Page.Resources>
        <local:booleanToVisibilityConverter x:Key="booleanToVisibilityConverter"/>
    </Page.Resources>-->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock Text="{Binding message}"></TextBlock>
        <HyperlinkButton Click="HyperlinkButton2_Click">Finish</HyperlinkButton>
        <!--<Button Click="{Binding CloseWindowCommand}">Finish</Button>-->
        <!--Visibility="{Binding isApproved, Converter={StaticResource BooleanToVisibilityConverter}}"-->
    </Grid>
</Page>
